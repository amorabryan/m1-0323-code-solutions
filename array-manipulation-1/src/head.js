/* exported head */

/* - PSEUDOCODE -
Define a function, named 'head', that accepts one argument, 'array', which is any JS Array.
  Declare a variable, named 'firstElement', and assign it to the first index of the 'array' argument.
  Return the value of the 'firstElement' variable.
 */

function head(array) {
  const firstElement = array[0];
  return firstElement;
}
